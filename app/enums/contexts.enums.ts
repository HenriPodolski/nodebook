export namespace ContextEnums {
	export enum js {
		server = 'server',
		client = 'client'
	}

	export enum ts {
		server = 'server',
		client = 'client'
	}

	export enum css {
		local = 'local',
		global = 'global'
	}
}

